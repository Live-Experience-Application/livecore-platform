# Documentation and backlog data are read-only reference material; they are
# not formatted by tooling.
docs/
csv/
LICENSE
# Per-package AGPL LICENSE copies (CORE-LIC-003) are the verbatim license text.
**/LICENSE
# The third-party NOTICE inventory is generated by
# scripts/generate-third-party-notices.ps1 and drift-gated; its formatting is owned
# by the generator (like the OpenAPI document), so Prettier must not reformat it.
THIRD-PARTY-NOTICES.md
**/THIRD-PARTY-NOTICES.md

# Lockfiles and generated output.
pnpm-lock.yaml
# NuGet lock files are machine-generated dependency manifests (CORE-DEP-003).
**/packages.lock.json
# The OpenAPI document is generated from the running route table (CORE-OAS-001);
# it is serialized by the API host, not formatted by Prettier.
openapi/
# The Kubernetes Helm chart (CORE-DEP-004) is a tooling-owned artifact: its
# templates are Go-templated manifests (`{{ ... }}`) that are NOT valid YAML and
# cannot be parsed by Prettier, and its formatting is validated by `helm lint`
# plus the `helm template` + kubeconform render gate (the helm-chart CI job), not
# by Prettier — the same posture as the generated OpenAPI document above.
deploy/helm/
# The OpenAPI-derived contract types are generated by openapi-typescript
# (CORE-OAS-002); their formatting is owned by the generator and drift-gated, so
# Prettier must not reformat them.
packages/contracts/src/openapi.ts
node_modules/
dist/
bin/
obj/
TestResults/
coverage/

# Local tooling state.
.claude/
