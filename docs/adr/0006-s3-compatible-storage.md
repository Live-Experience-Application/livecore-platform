# ADR 0006: S3-compatible Storage

## Status

Accepted for initial implementation.

## Decision

Assets use an S3-compatible abstraction so RustFS/self-hosted and provider-hosted storage can both work.

## Consequences

- All implementation must follow this decision until superseded by a later ADR.
- Any LLM-proposed change requires a new ADR and human approval.
