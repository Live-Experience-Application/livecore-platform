{
  "name": "@livecore/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Stable, product-neutral TypeScript contract types (DTOs, enums, events) for the LiveCore Core API, consumed by vertical apps.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && tsc -p tsconfig.test.json && node --test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
