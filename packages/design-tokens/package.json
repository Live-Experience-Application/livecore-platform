{
  "name": "@livecore/design-tokens",
  "version": "0.3.0",
  "description": "Generic, product-neutral design tokens and theme contracts for LiveCore vertical apps: the token contract (color, spacing, typography, radius, shadow, breakpoint, motion), the stable scale keys, a neutral base theme and a type-safe theme authoring helper.",
  "license": "AGPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Live-Experience-Application/livecore-platform.git",
    "directory": "packages/design-tokens"
  },
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "CHANGELOG.md",
    "LICENSE",
    "THIRD-PARTY-NOTICES.md"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && tsc -p tsconfig.test.json && node --test tests/design-tokens.build.test.mjs tests/design-tokens.pack.test.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
